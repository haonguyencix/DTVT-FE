import React from 'react';

const GradeItem = (props) => {
    const { item } = props;
    console.log("GradeItem -> item", item)
    return (
        <div>
            
        </div>
    );
};

export default GradeItem;